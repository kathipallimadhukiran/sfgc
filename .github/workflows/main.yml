name: Notification Alarm Clock

on:
  workflow_dispatch:

jobs:
  notification-check:
    runs-on: ubuntu-latest

    steps:
      - name: Test notification alarm
        run: |
          echo "======================================"
          echo "🤖 Notification Alarm Clock"
          echo "======================================"
          echo "⏰ GitHub Actions is working!"
          echo "📡 Ready to connect to Render"
          echo "======================================"
